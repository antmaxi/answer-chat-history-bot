# Copy to .env and fill in. All values are optional except the keys you need
# for the milestones you've reached: M1-M3 need none of them.

# DB_PATH=data/answerbot.db
# LOG_LEVEL=INFO
# LOG_PATH=                    # default: answerbot.log next to the DB; "off" for stderr only

# --- Embeddings (local) ---
# EMBED_MODEL=intfloat/multilingual-e5-small
# EMBED_DIM=384
# EMBED_THREADS=1
# HF_TOKEN=                    # optional; Hub token for gated models / rate limits
#                              # https://huggingface.co/settings/tokens (read)

# --- Windowing ---
# WINDOW_GAP_SECONDS=1800
# WINDOW_MAX_MSGS=25

# --- Speaker names ---
# SPEAKER_LABEL=name    # name (public name, User N fallback) | id (anonymous) | export (contact labels)

# --- Retrieval ---
# TOP_K=10
# MIN_K=10                # excerpts sent to the LLM; defaults match TOP_K
# MAX_K=10
# COSINE_MIN=0.7          # 0 disables the cosine cutoff (always send MAX_K)
# RECENCY_HALF_LIFE_DAYS=365  # 0 disables; time-range questions skip recency
# WEIGHT_VECTOR=1.0
# WEIGHT_KEYWORD=0.7
# STOPWORD_DF_RATIO=0.02  # drop query terms in more than this fraction of messages
# STOPWORD_KEEP=цюрих,швейцария  # never drop these (inflected forms included); "off" clears

# --- Answering (M4+) ---
# LLM_PROVIDER=claude          # claude | gemini | groq | openrouter | ollama | cursor
# ANTHROPIC_API_KEY=sk-ant-...
# GEMINI_API_KEY=              # or GOOGLE_API_KEY
# GROQ_API_KEY=
# OPENROUTER_API_KEY=
# CURSOR_API_KEY=              # from https://cursor.com/dashboard/api (Pro+)
#                              # venv: pip install 'answerbot[cursor]'
#                              # Docker image already includes cursor-sdk
# OPENROUTER_HTTP_REFERER=     # optional; OpenRouter uses it to attribute traffic
# OPENROUTER_APP_TITLE=answer-chat-history-bot
# ANSWER_MODEL=claude-sonnet-5 # groq: openai/gpt-oss-20b
#                              # openrouter: openai/gpt-oss-20b:free (or any model id)
#                              # gemini: gemini-2.5-flash
#                              # cursor: composer-2.5 (Cursor Models pool)
# ANSWER_MAX_TOKENS=8192       # Groq/OpenRouter; includes reasoning tokens
# ANSWER_MAX_REQUEST_TOKENS=0  # prompt+completion cap; Groq defaults to 8000 TPM
# ANSWER_COOLDOWN_SECONDS=8
# ANSWER_MAX_PER_USER_PER_HOUR=0  # LLM answers per user; 0 disables
# ANSWER_MAX_PER_HOUR=0           # LLM answers for the whole bot; 0 disables
# QUERY_LOG=1
# LLM_TIMEOUT=60
# OLLAMA_HOST=http://localhost:11434
# Inside Docker, Ollama on the host is http://host.docker.internal:11434

# --- Telegram bot (M5+) ---
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=            # main/control supergroup Bot API id, e.g. -1001234567890
#                              # a positive id is stored as -100<id>
#                              # commands and DMs are gated on membership in this chat
# TELEGRAM_CHAT_IDS=           # extra source chats (space- or comma-separated Bot API ids)
#                              # omitted = search and ingest only TELEGRAM_CHAT_ID
#                              # the bot must be a member (admin helps getChatMember) of each
# SEARCH_CHAT_SCOPE=main       # main = only TELEGRAM_CHAT_ID
#                              # all  = TELEGRAM_CHAT_ID plus TELEGRAM_CHAT_IDS
# SEARCH_CHAT_ACCESS=members   # members = secondary chats the asker currently belongs to
#                              # all     = every configured source (privacy: a main-chat
#                              #           member can search groups they are not in)
# ADMIN_USER_IDS=              # DMed up/down and ERROR logs
# GITHUB_REPO=https://github.com/antmaxi/answer-chat-history-bot
# DISPLAY_UTC_OFFSET_HOURS=2   # wall-clock times in /info (UTC+2)
# LIVE_REINDEX_EVERY=20
# LIVE_LOOKBACK_HOURS=6
# MEMBERSHIP_CACHE_SECONDS=300
# RESOLVE_DELAY_SECONDS=0.4    # pause between getChatMember calls in /resolve
# RESOLVE_MAX_FLOOD_WAIT=120   # pause the job if Telegram asks to wait longer
